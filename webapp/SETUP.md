# Настройка Mini App URL

Telegram Mini App требует HTTPS URL. Вот несколько способов получить его:

## Вариант 1: Локальная разработка с ngrok (быстро, для тестирования)

### Установка ngrok:
```bash
# macOS
brew install ngrok

# Или скачайте с https://ngrok.com/download
```

### Запуск:
1. Разместите файлы webapp на локальном сервере (например, Python):
```bash
cd /Users/volodazverev/priston_bot/webapp
python3 -m http.server 8000
```

2. В другом терминале запустите ngrok:
```bash
ngrok http 8000
```

3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

4. Добавьте в `.env`:
```
WEBAPP_URL=https://abc123.ngrok.io/
```

**⚠️ Внимание:** URL меняется при каждом перезапуске ngrok (бесплатная версия)

---

## Вариант 2: GitHub Pages (бесплатно, постоянно)

### Шаги:
1. Создайте репозиторий на GitHub (можно приватный)
2. Загрузите файлы из папки `webapp/` в репозиторий
3. В настройках репозитория → Pages:
   - Source: Deploy from a branch
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
4. Через несколько минут будет доступен URL:
   ```
   https://ваш-username.github.io/название-репозитория/
   ```

5. Добавьте в `.env`:
   ```
   WEBAPP_URL=https://ваш-username.github.io/название-репозитория/
   ```

---

## Вариант 3: Netlify (бесплатно, легко)

### Шаги:
1. Зарегистрируйтесь на https://www.netlify.com
2. Перейдите в "Add new site" → "Deploy manually"
3. Перетащите папку `webapp/` в браузер
4. Скопируйте URL (например: `https://random-name-123.netlify.app`)
5. Добавьте в `.env`:
   ```
   WEBAPP_URL=https://random-name-123.netlify.app/
   ```

---

## Вариант 4: Vercel (бесплатно, для разработчиков)

### Шаги:
1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. В папке webapp выполните:
   ```bash
   cd webapp
   vercel
   ```

3. Следуйте инструкциям
4. Скопируйте полученный URL в `.env`

---

## Вариант 5: Свой сервер (если есть)

### Если у вас есть сервер с доменом:

1. Загрузите файлы из `webapp/` на сервер:
   ```bash
   scp -r webapp/* user@your-server.com:/var/www/webapp/
   ```

2. Настройте веб-сервер (nginx/apache) для папки `/var/www/webapp/`

3. Настройте SSL (Let's Encrypt):
   ```bash
   certbot --nginx -d your-domain.com
   ```

4. Добавьте в `.env`:
   ```
   WEBAPP_URL=https://your-domain.com/webapp/
   ```

---

## Проверка работы

После настройки URL:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "📱 Открыть Mini App"
4. Должен открыться веб-интерфейс

---

## Важно для работы с API

Mini App пытается обращаться к API бота. Нужно либо:

1. **Создать API endpoints в боте** (через Flask/FastAPI)
2. **Использовать Telegram Bot API напрямую** (через long polling)
3. **Настроить CORS** на веб-сервере

**Для быстрого старта:** Можно использовать ngrok для тестирования, а потом перейти на постоянный хостинг.

