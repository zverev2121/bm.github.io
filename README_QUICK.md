# 🚀 Быстрый старт Mini App

## Самый простой способ (для тестирования):

### 1. Установите ngrok (если еще нет):
```bash
# macOS
brew install ngrok

# Или скачайте с https://ngrok.com
```

### 2. Запустите локальный сервер:
```bash
cd /Users/volodazverev/priston_bot/webapp
./quick_start.sh
```

Или вручную:
```bash
python3 -m http.server 8000
```

### 3. В другом терминале запустите ngrok:
```bash
ngrok http 8000
```

### 4. Скопируйте HTTPS URL из ngrok:
Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:8000
```

### 5. Добавьте в `.env`:
```env
WEBAPP_URL=https://abc123.ngrok.io/
```

### 6. Перезапустите бота:
```bash
python3 bot.py
```

### 7. Проверьте:
- Откройте бота в Telegram
- Отправьте `/start`
- Нажмите кнопку "📱 Открыть Mini App"

---

## ⚠️ Важно:

1. **ngrok URL меняется** при каждом перезапуске (бесплатная версия)
   - Решение: используйте ngrok с автологином для постоянного домена
   - Или перейдите на постоянный хостинг (GitHub Pages, Netlify)

2. **Для продакшена** используйте:
   - GitHub Pages (бесплатно)
   - Netlify (бесплатно)
   - Vercel (бесплатно)
   - Свой сервер с доменом

3. **API endpoints** - Mini App пока не подключен к API бота напрямую
   - Нужно будет добавить веб-сервер с API (Flask/FastAPI)
   - Или использовать Telegram Bot API через long polling

---

## 📝 Пример для постоянного домена (ngrok):

1. Зарегистрируйтесь на https://ngrok.com
2. Получите authtoken
3. Выполните:
   ```bash
   ngrok config add-authtoken YOUR_TOKEN
   ngrok http 8000 --domain=your-custom-domain.ngrok.io
   ```

4. Теперь URL будет постоянным!

